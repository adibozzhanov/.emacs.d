(recentf-mode 1) ; Keep track of what files were opened recently C-c f o is the keybind
(save-place-mode 1) ; Drop cursor in the last visited location
(global-auto-revert-mode 1) ; Auto revert files
(setq use-dialog-box nil) ; Don't show UI dialogs when prompting
(setq make-backup-files nil) ; Remove those annoying ~ files
