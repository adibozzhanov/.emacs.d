(use-package ligature
  :config
  (ligature-set-ligatures 'prog-mode '(("-" (rx (+ "-")))
                                       ("-" (rx (* "-") ">"))
                                       ("+" (rx (+ "+")))
                                       ("<" (rx (+ "=")))
                                       ("<" (rx (+ "=") ">"))
                                       ("<" (rx (+ "~")))
                                       ("<" (rx (+ "~") ">"))
                                       ("<" (rx "!" (+ "-")))
                                       ("<" (rx (+ "-")))
                                       ("<" (rx (+ "-") ">"))
                                       ("<" (rx "|"))
                                       (">" (rx (+ "=")))
                                       (">" (rx ">" (+ "=")))
                                       (">" (rx ">" (+ "=") ">"))
                                       (">" (rx (+ "-")))
                                       (">" (rx (+ "-") "<"))
                                       ("~" (rx (+ "~")))
                                       ("~" (rx (+ "~") ">"))
                                       ("=" (rx (+ "=")))
                                       "!="
                                       "!=="
                                       "[|"
                                       "|]"
                                       "{|"
                                       "|}"
                                       "|>"
                                       ))
  (global-ligature-mode t))
